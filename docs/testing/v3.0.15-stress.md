# v3.0.15 测试清单 — 外事号 5 字段 web 自助 + Sheet 同步

**版本**: v3.0.15(收紧版,只 ~250 行核心)
**ADR**: [0034](../adr/0034-v3.0.15-account-mgmt-sheet-sync.md)
**日期**: 2026-05-09
**预期总耗时**: 30-45 min

---

## 1. 静态自查(已通过 ✓)

```bash
python3 -m py_compile database.py auth_reset.py web.py bot.py tasks.py sheets.py
```

**关键 grep**:
```bash
# inspector_tg_id 全链路
grep -n "inspector_tg_id" database.py web.py sheets.py templates/index.html
# 同步 loop
grep -n "BUSINESS_FIELD_SYNC_ENABLED\|_sync_account_business_to_sheet" tasks.py
# audit / 中央台同步 — 应该没残留
grep -n "audit_log_db\|query_audit_logs\|MULTI_DEPT_AGGREGATE" *.py templates/*.html
# 应无输出(audit 全撤了,中央台同步留 v3.0.16)
```

---

## 2. 单元(已通过 ✓)

- migration V7: `accounts.inspector_tg_id` 添加 ✓
- `update_account_business` 扩 inspector_tg_id 参数 ✓
- v3.0.14 dedupe 回归(三叶辰 phone 后缀全局唯一)✓
- `_safe_add_column` race 容错 4 case 全过(Codex P0 fix)
- 幂等性:重跑 init_db user_version 不变 ✓

---

## 3. dev VPS 集成(待跑)

### 3.1 升级 dev VPS 到 v3.0.15

dev VPS 上 git checkout 这个 feature 分支(不走主流程 update.sh,因为不在 main):

```bash
ssh root@<dev-vps>
cd /root/tg-monitor-<dev>
git fetch origin feature/v3.0.15-account-mgmt-audit
git checkout feature/v3.0.15-account-mgmt-audit
docker compose up -d --build
```

**验证 migration**:
```bash
docker exec tg-monitor-<dev> sqlite3 /app/data/monitor.db \
  "PRAGMA user_version; .schema accounts | grep -E 'inspector_tg_id|operator|company';"
```
预期:
- `user_version = 7`
- accounts 多了 `inspector_tg_id TEXT DEFAULT ''`
- operator / company 老字段还在

### 3.2 web 编辑账号 + 60s Sheet 同步

1. 浏览器登 dev VPS web 后台
2. 账号管理 → 选号 → 点「配置」按钮
3. modal 顶部「业务归属」section 三字段填:
   - 商务人员:测试李四
   - 中心/部门:测试-运营
   - 监察员 TG:@test_inspector
4. 保存

**立刻验证 DB**:
```bash
docker exec tg-monitor-<dev> sqlite3 /app/data/monitor.db \
  "SELECT phone, name, operator, company, inspector_tg_id FROM accounts WHERE id=<X>;"
```
预期:operator='测试李四' company='测试-运营' inspector_tg_id='@test_inspector'

**等 60s 后看 Sheet**:
- B2 = 测试李四 ✓
- B3 = 测试-运营 ✓
- B4 = @test_inspector ✓

```bash
docker logs tg-monitor-<dev> --since 90s | grep "bus_sync"
```
预期出现:`[bus_sync] <tab_name> 同步 B2='测试李四', B3='测试-运营', B4='@test_inspector'`

### 3.3 P0 fix 验证:批量 batch_get(快速 + 不爆 quota)

```bash
docker logs tg-monitor-<dev> --since 5m | grep "bus_sync.*本轮共同步"
```
预期:`[bus_sync] 本轮共同步 N 个账号的业务字段(write M 次,read 1 次 batch)`

注意 `read 1 次 batch` — 不再是 200 次 batch_get。

### 3.4 P1 fix 验证:web 改 → 60s 内 Sheet 同步,清空也覆盖

1. web 改 operator = '清空测试' → 60s 后 Sheet B2 应该是 '清空测试'
2. web 改 operator = '' (清空) → 60s 后 Sheet B2 也应该清空(不是停留旧值)

### 3.5 P1 fix 验证:modal 加载未完成不允许保存

1. 浏览器 throttle 网络到 Slow 3G
2. 点「配置」按钮 → modal 出现 + 「保存」按钮 disabled(灰色)
3. 等加载完成 → 「保存」按钮变绿可点

### 3.6 反向覆盖(单向 web 优先)

1. Sheet 上手改 B2 = 'Sheet 手填' → 60s 后看 B2
2. 预期:B2 又变回 web 上的值(web 优先压回)

### 3.7 老分页升级

挑一个老分页(frozen<6 的阉割版,如果有):
```bash
docker exec tg-monitor-<dev> python3 -c "
import sheets, database as db
db.init_db()
w = sheets.SheetsWriter()
w.ensure_account_tabs()
"
```

**验证**:
- 老分页 frozen 仍 6
- A4 出现「监察员」label
- B4 等 60s 自动从 DB 填 inspector_tg_id
- Row 7+ 老消息**完全不动**(关键!检查 row 7-100)

---

## 4. 回归(不能破坏现有功能)

### 4.1 v3.0.14 dedupe 仍工作
有 2 个同名 accounts → ensure_account_tabs → 看是否仍 dedupe(应该正常)

### 4.2 老 /api/accounts/<id>/notify-config GET 行为不变
GET 应包含老字段(business_tg_id / owner_tg_id / remind_*_text) + 新字段(operator / company / inspector_tg_id)

### 4.3 BUSINESS_FIELD_SYNC_ENABLED=false 关回老行为
```bash
echo "BUSINESS_FIELD_SYNC_ENABLED=false" >> /app/.env
docker restart tg-monitor-<dev>
# Web 改 operator → 60s 不应有 [bus_sync] log
```

---

## 5. 回滚验证

模拟 v3.0.15 出问题,用户走 `bash rollback.sh` 回 v3.0.14:
1. accounts.inspector_tg_id 字段保留(SQLite ADD COLUMN 不破坏)
2. 老代码 v3.0.14 不读 inspector_tg_id 也不写,业务正常
3. Web modal 回到老状态,只能改两段式 @

---

## 6. 通过标准

- [x] 1 + 2 静态 + 单元全过
- [ ] 3.1-3.7 集成全过(dev VPS)
- [ ] 4.1-4.3 回归全过
- [ ] 24h 灰度无新 ERROR

满足后 → 用户 OK → ff-push main + tag v3.0.15。
